import { StyleSheet, Text, View } from 'react-native'

const saved = () => {
  return (
    <View>
      <Text>saved</Text>
    </View>
  )
}

export default saved

const styles = StyleSheet.create({})