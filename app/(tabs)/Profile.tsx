import { StyleSheet, Text, View } from "react-native";

const profile = () => {
  return (
    <View>
      <Text>profile</Text>
    </View>
  );
};

export default profile;

const styles = StyleSheet.create({});
